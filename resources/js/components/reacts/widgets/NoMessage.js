import React, { Component } from 'react';
import ReactDOM from 'react-dom';

export default function NoMessage(props) {
	return (
		<div className="no-message">
			There is no message here
		</div>
	);
}